# ── Required ──────────────────────────────────────────────────────────────────
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrSTUvwxyz
SOURCE_CHANNEL=@flow_alerts_channel
DEST_CHANNEL=@my_decisions_channel

# ── Mode ──────────────────────────────────────────────────────────────────────
# true  = bypass market data, send HOLD immediately after hard filter (local testing)
# false = full production logic
TEST_MODE=false

# ── Persistence (Railway only) ────────────────────────────────────────────────
# Mount a Railway Volume at /app/data, then set this so dedup survives redeploys.
# Leave unset for local development (defaults to data/signals.db).
# DB_PATH=/app/data/signals.db
